
let initialState = 0;

const changeTheNumber = (initialState,()=>{
    switch(){
        case "INCREMENT":
            return state = state+1

        case "DECREMENT":
            return state = state-1

        default: return state;
    }

})

export default changeTheNumber;